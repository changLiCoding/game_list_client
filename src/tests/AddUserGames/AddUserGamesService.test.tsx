import { renderHook } from "@testing-library/react";
import { useUserGames } from "../../services/userGames/useUserGames";

describe("Add User Games", () => {
	it("Should send add userGames request", async () => {});
});
