import { Game as GameType } from '@/graphql/__generated__/graphql';

function GameDetailHeaderInfo({ game }: { game: GameType | undefined }) {
  return <div>GameDetailInfo</div>;
}

export default GameDetailHeaderInfo;
