import { Game as GameType } from '@/graphql/__generated__/graphql';

function List({
  game,
  colorBgContainer,
}: {
  game: GameType;
  colorBgContainer: string;
}): JSX.Element {
  return <div>List</div>;
}

export default List;
