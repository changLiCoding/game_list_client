export type InitialStateType = {
  username: string;
  bannerPicture: string;
  createdAt: string;
  email: string;
  games: string[];
  isActive: boolean;
  userGames: string[];
  userPicture: string;
};
